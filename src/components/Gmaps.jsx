import React from 'react'

const Gmaps = () => {
  const fetchData = async () => {
    const resp = await axios.get(() => )
  }

  useEffect(() => {
    fetchData()
  })


  return <div></div>
}

export default Gmaps
